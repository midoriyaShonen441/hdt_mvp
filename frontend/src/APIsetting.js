const httpAPI = () => {
    const isAPI = "192.241.154.185:8888";
    return isAPI;
}

const dugAPI = () => {
    const isDebug = "http://localhost:8888"
    return isDebug;
}

export {httpAPI, dugAPI}