const httpAPI = () => {
    const isAPI = "http://localhost:8888";
    return isAPI;
}

const dugAPI = () => {
    const isDebug = "http://localhost:8888"
    return isDebug;
}

export {httpAPI, dugAPI}